const list=document.getElementById('tasks'),form=document.getElementById('form'),input=document.getElementById('task');function update(){let a=[...list.children],d=a.filter(x=>x.classList.contains('done')).length,p=a.length?Math.round(d/a.length*100):0;document.getElementById('count').textContent=`${d} of ${a.length} completed`;document.getElementById('pct').textContent=p+'%';document.getElementById('bar').style.width=p+'%'}function wire(x){x.querySelector('.check').onclick=()=>{x.classList.toggle('done');x.querySelector('.check').textContent=x.classList.contains('done')?'✓':'';update()};x.querySelector('.delete').onclick=()=>{x.remove();update()}}[...list.children].forEach(wire);form.onsubmit=e=>{e.preventDefault();let v=input.value.trim();if(!v)return;let x=document.createElement('li');x.innerHTML='<button class="check"></button><span></span><button class="delete">×</button>';x.querySelector('span').textContent=v;list.append(x);wire(x);input.value='';update()};document.querySelectorAll('.filter').forEach(b=>b.onclick=()=>{document.querySelectorAll('.filter').forEach(x=>x.classList.remove('active'));b.classList.add('active');let f=b.dataset.f;[...list.children].forEach(x=>x.style.display=f==='all'||f==='done'&&x.classList.contains('done')||f==='open'&&!x.classList.contains('done')?'flex':'none')});document.getElementById('theme').onclick=()=>document.body.classList.toggle('night');update();
